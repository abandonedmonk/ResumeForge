"""Gradio UI layer for ResumeForge.

``render`` holds pure presentation helpers (HTML/markdown builders, formatters);
``actions`` holds the callback functions that drive the pipeline. ``app.main``
assembles them into the Gradio ``Blocks`` (``build_ui``).
"""
